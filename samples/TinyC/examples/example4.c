{
    index = a = b = c = d = 0;

    {
        d = 199;

        ;
        ;
        ;
    }

    j = 128;
    i = 0;

    if (j * 2 == 128 << 1)
    {
        do
        {
            i = i + 1;
            j = j - 1;
        }
        while (i <= j);
    }

    x = !0;
    y = !1;
    z = ~x + ~y ? a : b;
    n = z
        ? a < 0 ? b + 100 * c : c % 100 + 1
        : b > 0 ? x : (y = 23 & -7);
}
